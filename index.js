function shout(str) {
  return str.toUpperCase()
}

function whisper(str) {
  return str.toUpperCase()
}

function logShout(str) {
  console.log(str.toUpperCase())
}

function shout(str) {
  return str.toUpperCase()
}